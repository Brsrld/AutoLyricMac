# AutoLyricMac engine dependencies — Step 2 (kept minimal on purpose).
# yt-dlp is used as a modular local source adapter for authorized media only.
# FFmpeg/ffprobe come from Homebrew, not pip.
yt-dlp>=2026.7.4
# Rendering (Phase 0 prototypes onward)
Pillow>=11.0
numpy>=2.0
# Audio analysis (Phase 2)
librosa>=0.11
soundfile>=0.12
# Lyric alignment (Phase 3) — Apple-Silicon Whisper; model weights
# (mlx-community/whisper-base-mlx, ~150 MB) download on first use with
# explicit user approval.
mlx-whisper>=0.4
# Automatic Turkish translation — local/offline Argos models (no LLM/API);
# language-pair models (~200 MB each) download once on first use.
argostranslate>=1.9
